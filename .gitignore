# Bí mật — TUYỆT ĐỐI không đẩy lên GitHub
.env
*.env
secrets*
*client_secret*
*api_key*

# Output & dữ liệu chạy
web_runs/
template_store/
Result/

# Dữ liệu hợp đồng/template THẬT — KHÔNG đẩy lên GitHub public (lộ dữ liệu + vi phạm luật)
contracts/
template/

# Bí mật do bộ skill AgentBase tạo khi deploy — TUYỆT ĐỐI không đẩy lên GitHub
.greennode.json
*.greennode.json

# Bộ skill AgentBase (không cần nằm trong repo nộp bài)
greennode-agentbase-skills-main/
greennode-agentbase-skills/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Hệ điều hành
.DS_Store
Thumbs.db
.agentbase/
